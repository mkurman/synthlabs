export enum FeedDisplayMode {
  Default = 'default',   // Full stacked cards with all details
  List = 'list',         // Compact expandable rows (database-like)
  Cards = 'cards'        // Shorter compact cards
}
